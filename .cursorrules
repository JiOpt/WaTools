# Cursor Rules — MyTooLife（靜態站 SEO 與結構）

## 1. 專案與技術棧

- **MyTooLife**：靜態 HTML + Bootstrap + 原生 JS，部署於 Firebase Hosting（`cleanUrls`）
- 正式網域：`https://mytoolife.com`
- **不是** Next.js；勿引入 App Router / `layout.js` 等 Next 專用模式
- 全站局部換頁：`assets/js/soft-nav.js`（只換 `<main>`，header/footer 常駐）

## 2. 核心開發哲學

- 最小必要 diff；沿用既有 `layout-shell.mjs`、`seo-meta.mjs` 慣例
- 效能與可維護性優先（defer 腳本、版本號 `?v=` 快取刷新）
- 半年後仍易讀：SEO 規則集中在 `scripts/seo-meta.mjs`，勿在各 HTML 手寫重複 meta

## 3. 新增子頁面（工具／經文／靜態頁）規範

### A. 語意化 HTML

- 每頁一個 `<main>`、一個 `<h1>`（工具頁為 `#tool-page-title`）
- 標題階層依序 `<h2>` → `<h3>`，不可跳階

### B. SEO Meta（必須）

**工具頁**：用 `scripts/generate-tool-pages.mjs` 產生，或手動對齊 `seo-meta.mjs` 輸出：

- `title`：45–60 字元，`buildToolPageTitle()`
- `description`：110–150 字元，`buildPageDescription()`
- `canonical`：`https://mytoolife.com/{category}/{slug}`（**無 `.html`**，與 Firebase `cleanUrls` 一致）
- `meta robots`：預設 `index, follow`；設定等工具頁用 `NOINDEX_SLUGS`
- Open Graph + Twitter Card：透過 `renderSeoMeta()`
- JSON-LD：工具 `WebApplication`（`renderWebApplicationSchema`）；經文 `Article`（`renderArticleSchema`）

**經文頁**：執行 `scripts/build-scriptures.mjs`（已整合 `seo-meta.mjs`）

**首頁**：`index.html` 內含 JSON-LD `WebSite`；執行 `generate-index-seo.mjs` 維護靜態爬蟲目錄

### C. 不要索引的頁面

- 將 slug 加入 `scripts/seo-meta.mjs` → `NOINDEX_SLUGS`
- 重跑 `npm run seo:tools`
- 勿把純內部頁（如 `index_plan.html`）加入 `sitemap.xml`

### D. 圖片與連結

- `<img>` 必有描述性 `alt`（logo 用品牌名）
- 外部連結加 `rel="noopener noreferrer"`（若 `target="_blank"`）
- 站內連結優先 **clean URL**：`/life/calendar` 而非 `calendar.html`

## 4. SEO 建置流程（新增／修改內容後）

```bash
npm run sitemap:build   # 若動了 sitemap.txt
npm run seo:build       # sitemap.xml + 首頁爬蟲目錄 + 工具頁 meta
```

| 變更類型 | 額外步驟 |
|----------|----------|
| 新工具 slug | `tools-data.js` → `generate-tool-pages.mjs` → `sitemap.txt` → `seo:build` |
| 新經文 | `build-scriptures.mjs` → `seo:sitemap` + `seo:index` |
| 改 SEO 規則 | 改 `seo-meta.mjs` → `seo:build` |
| soft-nav 換頁 meta | 改 `soft-nav.js` 的 `syncMeta()`，保持與 head 標籤一致 |

Deploy 前：版本 bump → `stamp-asset-version.mjs`（JS 預設；CSS 才加 `--html`）→ `firebase deploy --project watoolio`

## 9. 首頁工具卡片瀏覽人次（Firebase）

- API：`/api/pageviews`（Cloud Function `pageviews` + Firestore `pageviews/{slug}`）
- 前端：`assets/js/page-views.js`；卡片 UI 在 `catalog-render.js`
- 卡片顯示 `views`（總瀏覽次數）；進入工具頁 POST 記錄
- 部署：`firebase deploy --only functions,hosting --project watoolio`

## 5. soft-nav 與 SEO

- 爬蟲直接請求 URL 時仍收到完整 HTML（不受 soft-nav 影響）
- 使用者站內換頁時，`soft-nav.js` 必須同步：`title`、`description`、`canonical`、OG、Twitter、JSON-LD
- 新增 head 標籤類型時，一併更新 `syncMeta()`

## 6. 局部渲染與狀態（本專案實作）

- Header、左側網站地圖、Footer **不**隨換頁重載；僅 `<main class="main">` 替換
- 換頁後呼叫 `__waCloseHeaderOverlays()` 關閉手機選單／下拉
- 動態工具內容載入後需觸發 `mytoolife:soft-nav` 或對應 boot 函式

## 7. SEO 檢查清單（Review 時）

1. canonical 是否為 clean URL（無 `.html`）且與 `sitemap.xml` `<loc>` 一致
2. JSON-LD 的 `url` 是否與 canonical 相同（`pageUrl()` 會去掉 `.html`）
3. 首頁是否有 `seo-crawler-nav` 靜態連結（`generate-index-seo.mjs`）
4. 新開放工具是否已寫入 `sitemap.txt` 並重跑 `seo:build`
5. `settings` 等頁是否維持 `noindex`
6. 圖片 `alt`、對比度、defer 腳本是否影響 LCP
7. `robots.txt` 是否仍指向正確 `sitemap.xml`

## 8. 記憶體與事件清理

- `addEventListener` / `setInterval` 在 SPA 換頁或元件卸載時必須清理
- 站內地圖、工具 boot 使用 `__waCancelToolBoot` 避免重複初始化
